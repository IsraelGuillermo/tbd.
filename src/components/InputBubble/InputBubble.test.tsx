import React from "react"
import { InputBubble } from "./InputBubble"

describe("InputBubble", () => {
  it("is defined", () =>
    expect(
      <InputBubble width={100} placeholder={"# of options"} />,
    ).toBeDefined())

  it.todo(`add meaningful tests 👍`)
})
